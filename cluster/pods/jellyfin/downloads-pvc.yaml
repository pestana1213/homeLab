apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-pvc-new
  namespace: playground
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi   # desired size
  storageClassName: local-path